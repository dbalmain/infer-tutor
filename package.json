{
  "name": "infer-tutor",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "bun --filter @infer-tutor/web dev",
    "build": "bun --filter @infer-tutor/web build",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^7.0.2"
  }
}
